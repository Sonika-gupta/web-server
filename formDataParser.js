module.exports = function (data, boundary) {
  const body = ''
  //   while (data) {
  // take in chunks till boundary
  // split headers and content with double crlf
  // get content disposition : name and other fields
  // get content type
  // combine the data according to content type and save to body
  //   }
  return body
}
